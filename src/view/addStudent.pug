block content

     mixin inputGroup(name, label)
        -
            const nameToUpperCase = name[0];
        label
            | #{nameToUpperCase}
            input.form-control(name=name, type='text')

    form(action="/addStudent", method="POST")
        +inputGroup("name", "Nom")
        +inputGroup("birth", "Date de naissance")

        button(type="submit") Ajouter